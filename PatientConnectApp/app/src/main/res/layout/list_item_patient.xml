<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:layout_gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        card_view:cardCornerRadius="4dp"
        card_view:cardElevation="2dp"
        android:foreground="?android:attr/selectableItemBackground">
    <RelativeLayout android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginLeft="6dp" android:layout_marginRight="6dp"
                    android:layout_marginTop="6dp" android:layout_marginBottom="6dp">
        <TextView android:id="@+id/patient_name"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  style="@android:style/TextAppearance.Material.Title"
                  android:layout_alignParentStart="true"/>

        <!-- Last Reported (painLevel) pain on (timestamp) -->
        <!-- Has Reported (painLevel) pain since (timestamp) -->
        <TextView android:id="@+id/reported_since_summary"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  style="@android:style/TextAppearance.Material.Caption"
                  android:labelFor="@+id/timestamp"
                  android:layout_alignParentStart="true"
                  android:layout_below="@+id/patient_name"/>

        <TextView android:id="@+id/last_reported_summary"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  style="@android:style/TextAppearance.Material.Caption"
                  android:labelFor="@+id/timestamp"
                  android:layout_alignParentStart="true"
                  android:layout_below="@+id/reported_since_summary"/>


    </RelativeLayout>
</android.support.v7.widget.CardView>